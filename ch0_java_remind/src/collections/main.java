package collections;

import java.util.*;

public class main {
    public static void main(String[] args) {
        Integer[] arr = {0, 4, 2, 5, 1};

        ArrayList<Integer> list = new ArrayList<>(Arrays.asList(arr));
        list.add(5);
    }
}
